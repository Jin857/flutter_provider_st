import 'package:flutter/material.dart';

class PointGamePage extends StatelessWidget {
  const PointGamePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("五子棋"),
      ),
    );
  }
}
