import 'package:flutter/cupertino.dart';

class GomokuMain extends StatelessWidget {
  const GomokuMain({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
